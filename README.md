# Weight-Discrepancy
## Cómo correr la app

1) Instala dependencias:
```bash
pip install -r requirements.txt
streamlit run app.py

Luego guardas (commit).

---

## Opción B) Lo corres en tu computador (terminal)
1) **Clona** el repo (botón verde “Code” → copia URL)
2) En tu terminal:

```bash
git clone https://github.com/TU_USUARIO/Weight-Discrepancy.git
cd Weight-Discrepancy
pip install -r requirements.txt
streamlit run app.py
